openapi: 3.0.3
info:
  title: Dangling API
  version: "1.0"
paths:
  /things:
    get:
      responses:
        "200":
          description: Every thing.
          content:
            application/json:
              schema:
                type: object
                properties:
                  owner: { $ref: "#/components/schemas/Missing" }
